import 'package:flutter/material.dart';

class FavoritesPages extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('im favorites'),
    );
  }
}
